## 2024-05-14 - Add focus visible styles and ARIA labels
**Learning:** Adding consistent focus indicators and ARIA labels for icon-only components is a common accessibility improvement across projects. A global `:focus-visible` ensures consistency.
**Action:** When reviewing future repositories, check for both `:focus-visible` global styles and `aria-label` attributes on icon-only buttons (`<button><Icon/></button>`).
